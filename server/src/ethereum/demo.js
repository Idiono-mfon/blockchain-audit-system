


      // const promise = new Promise((resolve, reject) => {
      //   const logContents = getLogFromBlockchain(fileId, parseInt(fLogCount[index], 10));
      //   resolve(logContents);
      //   // (null); //Come back and check for errors
      // });
      // Return promise
      // return promise
      //   .then((contentRes) => {
      //     logs.push({
      //       content: decryptContentToStr(contentRes),
      //       file: {
      //         fileName: base32ToStr(fName[index]),
      //         size: base32ToStr(fSize[index]),
      //         ipAddress: base32ToStr(fIpAddress[index]),
      //         date: fDate[index],
      //       },
      //     });
      // })
      // .catch((error) => {
      //   console.log(error);
      // });
      // const logContents = await getlog(fileId, parseInt(fLogCount[index]));